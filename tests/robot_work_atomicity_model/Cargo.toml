[workspace]

[package]
name = "robot-work-atomicity-model"
version = "0.1.0"
edition = "2024"
publish = false

[lints.rust]
unsafe_code = "forbid"

[dependencies]
frankenterm-core = { path = "../../crates/frankenterm-core", default-features = false }
serde_json = "1.0.145"
stateright = "0.31.0"
